import datetime, calendar

a = {date_attempted = 11-02-2011}
b = {date_attempted = 09-10-2011}

logs = [a, b]

loglist = dict()

for log in logs:
    if log.date_attempted in loglist:
    loglist[log.date_attempted] += 1
    else:
    loglist.append(log.date_attempted = 1)

nms = calendar.month_name
this_year = datetime.date.today().year
self.dateset = []
for month in range(1,12):
    mc = calendar.monthcalendar(this_year, month)
    for d in datelist:
    for week in mc:
        if d['the_date'].month == month and d['the_date'].day in week:
        the_tuple = tuple('week of '+nms[month], week[0], d['count'])    
        self.dateset.append(the_tuple)
